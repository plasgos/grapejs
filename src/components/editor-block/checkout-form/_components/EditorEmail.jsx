import BasicInputProps from "./BasicInputProps";

const EditorEmail = ({ item, handleContentChange }) => {
  return (
    <BasicInputProps item={item} handleContentChange={handleContentChange} />
  );
};

export default EditorEmail;
